let numero = 3;


if (numero > 0) {
    console.log(`$(numeroPositivo) é positivo`);
} else if (numero < 0) {
    console.log(`$(numeroNegativo) é negativo`)
} else {
    console.log(`$(numeroNeutro) é zero`);
}